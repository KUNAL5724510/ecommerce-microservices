server:
  port: 8888

spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/KUNAL5724510/ecommerce-config-repo
          clone-on-start: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
